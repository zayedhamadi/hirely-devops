apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: auth-hirely-tls
  namespace: hirely
spec:
  secretName: auth-hirely-tls-secret
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
  dnsNames:
    - auth.hirely.local
